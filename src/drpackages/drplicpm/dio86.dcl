declare
        allvec  entry ((2) pointer),
        getdpb  entry ((2) pointer),
        movgtl	entry (fixed,(2) pointer,pointer),
	movltg	entry (fixed,pointer, (2) pointer),
	movgtg	entry (fixed,(2) pointer,(2) pointer);
